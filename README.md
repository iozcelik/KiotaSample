Sample repository for the blog post.

The "generated" branch is created by Kiota and generates errors when building the solution. In contrast, the "master" branch is fixed and includes some missing methods.
